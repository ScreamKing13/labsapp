<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myprogs.labsapp">

    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Lab5Activity"></activity>
        <activity android:name=".Lab4Activity" />
        <activity android:name=".InterGraphicsActivity" />
        <activity android:name=".Lab3Activity" />
        <activity android:name=".GraphicsActivity" />
        <activity android:name=".Lab2Activity" />
        <activity android:name=".StudentInfoActivity" />
        <activity android:name=".AlgImageActivity" />
        <activity android:name=".CycledActivity" />
        <activity android:name=".RamifiedActivity" />
        <activity android:name=".LinearActivity" />
        <activity android:name=".Lab1Activity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>